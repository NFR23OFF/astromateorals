<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AstroMate Orals - Premium Chief Mate Prep Dashboard</title>
    <style>
        :root { 
            --primary: #0a192f; 
            --accent: #64ffda; 
            --text: #8892b0; 
            --white: #e6f1ff; 
            --card-bg: #112240; 
            --dark-blue: #020c1b;
            --danger: #ff4d4d;
            --warning: #ffb703;
        }
        body { 
            font-family: 'Segoe UI', system-ui, sans-serif; 
            background: var(--primary); 
            margin: 0; 
            padding: 0; 
            color: var(--text); 
            padding-bottom: 60px; 
        }
        header { 
            background: var(--dark-blue); 
            padding: 30px 20px; 
            text-align: center; 
            border-bottom: 2px solid var(--card-bg); 
        }
        header h1 { 
            margin: 0; 
            font-size: 28px; 
            color: var(--white); 
            font-weight: 700; 
            letter-spacing: 1px; 
        }
        .subtitle { 
            font-size: 13px; 
            color: var(--accent); 
            margin-top: 5px; 
            font-weight: 600; 
            text-transform: uppercase; 
            letter-spacing: 2px;
        }
        
        .main-layout {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
        }
        @media(min-width: 900px) {
            .main-layout {
                grid-template-columns: 320px 1fr;
            }
            .sidebar-controls {
                position: sticky;
                top: 20px;
                height: max-content;
            }
        }

        .control-panel {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 20px;
            border: 1px solid #233554;
            margin-bottom: 20px;
        }
        .control-panel h3 {
            color: var(--white);
            margin-top: 0;
            margin-bottom: 15px;
            font-size: 16px;
            border-bottom: 1px solid #233554;
            padding-bottom: 8px;
        }

        .directory-box label { 
            display: block; 
            color: var(--white); 
            font-size: 13px; 
            font-weight: 600; 
            margin-bottom: 8px; 
        }
        .directory-select { 
            width: 100%; 
            padding: 12px 15px; 
            background: var(--primary); 
            border: 1px solid #233554; 
            border-radius: 6px; 
            color: var(--white); 
            font-size: 14px; 
            outline: none; 
            margin-bottom: 15px; 
            cursor: pointer; 
        }
        .directory-select:focus {
            border-color: var(--accent);
        }
        
        .profile-card { 
            background: #172a45; 
            border: 1px solid var(--accent); 
            border-radius: 8px; 
            padding: 15px; 
            margin-bottom: 20px;
            display: none; 
        }
        .profile-title { color: var(--white); font-size: 16px; font-weight: bold; margin-bottom: 5px; }
        .profile-meta { color: var(--accent); font-size: 12px; font-weight: 600; }

        .search-box { margin-bottom: 20px; }
        .search-box input { 
            width: 100%; 
            padding: 14px 20px; 
            background: var(--card-bg); 
            border: 1px solid #233554; 
            border-radius: 8px; 
            color: var(--white); 
            font-size: 15px; 
            box-sizing: border-box; 
            outline: none; 
        }
        .search-box input:focus { border-color: var(--accent); }

        .tabs-container {
            display: flex;
            background: var(--dark-blue);
            padding: 5px;
            border-radius: 8px;
            margin-bottom: 20px;
            border: 1px solid #233554;
        }
        .tab-btn {
            flex: 1;
            text-align: center;
            padding: 10px;
            color: var(--text);
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            border-radius: 6px;
            transition: 0.2s;
        }
        .tab-btn.active {
            background: var(--card-bg);
            color: var(--accent);
        }

        .filter-group {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-bottom: 15px;
        }
        .filter-chip { 
            background: var(--primary); 
            border: 1px solid #233554; 
            color: var(--white); 
            padding: 6px 12px; 
            border-radius: 20px; 
            font-weight: 600; 
            cursor: pointer; 
            transition: 0.2s; 
            font-size: 11px; 
        }
        .filter-chip.active, .filter-chip:hover { border-color: var(--accent); color: var(--accent); background: rgba(100, 255, 218, 0.05); }
        
        .stats-banner {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-bottom: 20px;
        }
        .stat-box {
            background: var(--card-bg);
            border: 1px solid #233554;
            border-radius: 8px;
            padding: 12px;
            text-align: center;
        }
        .stat-val {
            font-size: 20px;
            font-weight: 700;
            color: var(--accent);
        }
        .stat-lbl {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 4px;
        }

        .card { 
            background: var(--card-bg); 
            border-radius: 12px; 
            padding: 24px; 
            margin-bottom: 20px; 
            box-shadow: 0 10px 30px -15px rgba(2,12,27,0.7); 
            border-top: 4px solid #233554; 
            position: relative; 
            transition: 0.3s; 
        }
        .card.hot { border-top-color: var(--danger); }
        .card.hot::after { 
            content: '⚠️ SAFETY CRITICAL'; 
            position: absolute; 
            top: 24px; 
            right: 24px; 
            color: var(--danger); 
            font-size: 10px; 
            font-weight: bold; 
            letter-spacing: 1px; 
        }
        
        .meta-info { 
            font-size: 11px; 
            color: var(--accent); 
            margin-bottom: 12px; 
            font-weight: 600; 
            text-transform: uppercase; 
            letter-spacing: 1px; 
            display: flex;
            gap: 15px;
        }
        .question-text { font-size: 16px; font-weight: 600; color: var(--white); margin-bottom: 18px; line-height: 1.5; }
        
        .action-btn { 
            background: transparent; 
            border: 1px solid var(--accent); 
            color: var(--accent); 
            padding: 10px 15px; 
            border-radius: 4px; 
            font-weight: 600; 
            cursor: pointer; 
            width: 100%; 
            font-size: 13px; 
            transition: 0.3s; 
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .action-btn:hover { background: rgba(100, 255, 218, 0.1); }
        
        .answer-box { 
            display: none; 
            margin-top: 20px; 
            border-top: 1px solid #233554; 
            padding-top: 20px; 
        }
        .answer-title { font-weight: bold; color: var(--accent); margin-bottom: 8px; font-size: 12px; letter-spacing: 0.5px; text-transform: uppercase; }
        .answer-content { font-size: 14.5px; line-height: 1.6; color: var(--text); }
        
        .fail-box { 
            background: rgba(255, 77, 77, 0.07); 
            border: 1px solid var(--danger); 
            color: #ff6b6b; 
            padding: 14px; 
            border-radius: 6px; 
            margin-top: 15px; 
            font-size: 13px; 
            line-height: 1.5; 
        }
        .fail-box strong {
            color: var(--danger);
            text-transform: uppercase;
            font-size: 11px;
            display: block;
            margin-bottom: 4px;
            letter-spacing: 0.5px;
        }
        .reg-badge { 
            display: inline-block; 
            background: #172a45; 
            color: var(--white); 
            font-size: 11px; 
            padding: 4px 10px; 
            border-radius: 4px; 
            margin-top: 15px; 
            font-weight: 600; 
            border: 1px solid #233554; 
        }

        /* FAQ Styling System */
        .faq-container {
            display: none;
        }
        .faq-item {
            background: var(--card-bg);
            border: 1px solid #233554;
            border-radius: 8px;
            margin-bottom: 12px;
            overflow: hidden;
        }
        .faq-trigger {
            padding: 18px 20px;
            color: var(--white);
            font-weight: 600;
            font-size: 15px;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #112240;
            user-select: none;
        }
        .faq-trigger:hover {
            background: #172a45;
        }
        .faq-content {
            display: none;
            padding: 20px;
            border-top: 1px solid #233554;
            font-size: 14px;
            line-height: 1.6;
            background: rgba(2, 12, 27, 0.3);
        }

        .empty-state {
            text-align: center;
            padding: 40px;
            background: var(--card-bg);
            border-radius: 12px;
            border: 1px dashed #233554;
        }
    </style>
</head>
<body>

    <header>
        <h1>AstroMate Orals</h1>
